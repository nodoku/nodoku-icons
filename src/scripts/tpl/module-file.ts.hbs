import type {IconType} from "react-icons";
{{#numbers}}
import { nameToReactIcon_{{module}}_{{.}} } from "./name-to-react-icon-{{module}}-{{.}}";
{{/numbers}}

export async function nameToReactIcon_{{module}}(iconName: string): Promise<IconType | undefined> {

    return (
    {{#numbers}}
        await nameToReactIcon_{{module}}_{{.}}(iconName) ||
    {{/numbers}}
        undefined
    );

}