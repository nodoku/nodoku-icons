import type {IconType} from "react-icons";

import {
{{#icons}}
    {{.}},
{{/icons}}
} from "react-icons/{{module}}"

export function nameToReactIcon_{{module}}_{{fileIndex}}(iconName: string): IconType | undefined {

    switch(iconName) {
    {{#icons}}
        case "{{module}}:{{.}}":
            return {{.}};
    {{/icons}}
    }
}
