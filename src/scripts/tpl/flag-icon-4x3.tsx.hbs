import {JSX} from "react"
import {IconBaseProps} from "react-icons/lib";

export function {{fnName}}(props: IconBaseProps, className: string): JSX.Element {
    return (
        {{{svg_4x3}}}
    );
}
