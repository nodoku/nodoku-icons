import {JSX} from "react";
import {IconType} from "react-icons/lib";
import {IconBaseProps} from "react-icons/lib";

import { ad_flag } from "./ad"
import { ae_flag } from "./ae"
import { af_flag } from "./af"
import { ag_flag } from "./ag"
import { ai_flag } from "./ai"
import { al_flag } from "./al"
import { am_flag } from "./am"
import { ao_flag } from "./ao"
import { aq_flag } from "./aq"
import { ar_flag } from "./ar"
import { arab_flag } from "./arab"
import { as_flag } from "./as"
import { at_flag } from "./at"
import { au_flag } from "./au"
import { aw_flag } from "./aw"
import { ax_flag } from "./ax"
import { az_flag } from "./az"
import { ba_flag } from "./ba"
import { bb_flag } from "./bb"
import { bd_flag } from "./bd"
import { be_flag } from "./be"
import { bf_flag } from "./bf"
import { bg_flag } from "./bg"
import { bh_flag } from "./bh"
import { bi_flag } from "./bi"
import { bj_flag } from "./bj"
import { bl_flag } from "./bl"
import { bm_flag } from "./bm"
import { bn_flag } from "./bn"
import { bo_flag } from "./bo"
import { bq_flag } from "./bq"
import { br_flag } from "./br"
import { bs_flag } from "./bs"
import { bt_flag } from "./bt"
import { bv_flag } from "./bv"
import { bw_flag } from "./bw"
import { by_flag } from "./by"
import { bz_flag } from "./bz"
import { ca_flag } from "./ca"
import { cc_flag } from "./cc"
import { cd_flag } from "./cd"
import { cefta_flag } from "./cefta"
import { cf_flag } from "./cf"
import { cg_flag } from "./cg"
import { ch_flag } from "./ch"
import { ci_flag } from "./ci"
import { ck_flag } from "./ck"
import { cl_flag } from "./cl"
import { cm_flag } from "./cm"
import { cn_flag } from "./cn"
import { co_flag } from "./co"
import { cp_flag } from "./cp"
import { cr_flag } from "./cr"
import { cu_flag } from "./cu"
import { cv_flag } from "./cv"
import { cw_flag } from "./cw"
import { cx_flag } from "./cx"
import { cy_flag } from "./cy"
import { cz_flag } from "./cz"
import { de_flag } from "./de"
import { dg_flag } from "./dg"
import { dj_flag } from "./dj"
import { dk_flag } from "./dk"
import { dm_flag } from "./dm"
import { do_flag } from "./do"
import { dz_flag } from "./dz"
import { eac_flag } from "./eac"
import { ec_flag } from "./ec"
import { ee_flag } from "./ee"
import { eg_flag } from "./eg"
import { eh_flag } from "./eh"
import { er_flag } from "./er"
import { es_ct_flag } from "./es-ct"
import { es_ga_flag } from "./es-ga"
import { es_pv_flag } from "./es-pv"
import { es_flag } from "./es"
import { et_flag } from "./et"
import { eu_flag } from "./eu"
import { fi_flag } from "./fi"
import { fj_flag } from "./fj"
import { fk_flag } from "./fk"
import { fm_flag } from "./fm"
import { fo_flag } from "./fo"
import { fr_flag } from "./fr"
import { ga_flag } from "./ga"
import { gb_eng_flag } from "./gb-eng"
import { gb_nir_flag } from "./gb-nir"
import { gb_sct_flag } from "./gb-sct"
import { gb_wls_flag } from "./gb-wls"
import { gb_flag } from "./gb"
import { gd_flag } from "./gd"
import { ge_flag } from "./ge"
import { gf_flag } from "./gf"
import { gg_flag } from "./gg"
import { gh_flag } from "./gh"
import { gi_flag } from "./gi"
import { gl_flag } from "./gl"
import { gm_flag } from "./gm"
import { gn_flag } from "./gn"
import { gp_flag } from "./gp"
import { gq_flag } from "./gq"
import { gr_flag } from "./gr"
import { gs_flag } from "./gs"
import { gt_flag } from "./gt"
import { gu_flag } from "./gu"
import { gw_flag } from "./gw"
import { gy_flag } from "./gy"
import { hk_flag } from "./hk"
import { hm_flag } from "./hm"
import { hn_flag } from "./hn"
import { hr_flag } from "./hr"
import { ht_flag } from "./ht"
import { hu_flag } from "./hu"
import { ic_flag } from "./ic"
import { id_flag } from "./id"
import { ie_flag } from "./ie"
import { il_flag } from "./il"
import { im_flag } from "./im"
import { in_flag } from "./in"
import { io_flag } from "./io"
import { iq_flag } from "./iq"
import { ir_flag } from "./ir"
import { is_flag } from "./is"
import { it_flag } from "./it"
import { je_flag } from "./je"
import { jm_flag } from "./jm"
import { jo_flag } from "./jo"
import { jp_flag } from "./jp"
import { ke_flag } from "./ke"
import { kg_flag } from "./kg"
import { kh_flag } from "./kh"
import { ki_flag } from "./ki"
import { km_flag } from "./km"
import { kn_flag } from "./kn"
import { kp_flag } from "./kp"
import { kr_flag } from "./kr"
import { kw_flag } from "./kw"
import { ky_flag } from "./ky"
import { kz_flag } from "./kz"
import { la_flag } from "./la"
import { lb_flag } from "./lb"
import { lc_flag } from "./lc"
import { li_flag } from "./li"
import { lk_flag } from "./lk"
import { lr_flag } from "./lr"
import { ls_flag } from "./ls"
import { lt_flag } from "./lt"
import { lu_flag } from "./lu"
import { lv_flag } from "./lv"
import { ly_flag } from "./ly"
import { ma_flag } from "./ma"
import { mc_flag } from "./mc"
import { md_flag } from "./md"
import { me_flag } from "./me"
import { mf_flag } from "./mf"
import { mg_flag } from "./mg"
import { mh_flag } from "./mh"
import { mk_flag } from "./mk"
import { ml_flag } from "./ml"
import { mm_flag } from "./mm"
import { mn_flag } from "./mn"
import { mo_flag } from "./mo"
import { mp_flag } from "./mp"
import { mq_flag } from "./mq"
import { mr_flag } from "./mr"
import { ms_flag } from "./ms"
import { mt_flag } from "./mt"
import { mu_flag } from "./mu"
import { mv_flag } from "./mv"
import { mw_flag } from "./mw"
import { mx_flag } from "./mx"
import { my_flag } from "./my"
import { mz_flag } from "./mz"
import { na_flag } from "./na"
import { nc_flag } from "./nc"
import { ne_flag } from "./ne"
import { nf_flag } from "./nf"
import { ng_flag } from "./ng"
import { ni_flag } from "./ni"
import { nl_flag } from "./nl"
import { no_flag } from "./no"
import { np_flag } from "./np"
import { nr_flag } from "./nr"
import { nu_flag } from "./nu"
import { nz_flag } from "./nz"
import { om_flag } from "./om"
import { pa_flag } from "./pa"
import { pc_flag } from "./pc"
import { pe_flag } from "./pe"
import { pf_flag } from "./pf"
import { pg_flag } from "./pg"
import { ph_flag } from "./ph"
import { pk_flag } from "./pk"
import { pl_flag } from "./pl"
import { pm_flag } from "./pm"
import { pn_flag } from "./pn"
import { pr_flag } from "./pr"
import { ps_flag } from "./ps"
import { pt_flag } from "./pt"
import { pw_flag } from "./pw"
import { py_flag } from "./py"
import { qa_flag } from "./qa"
import { re_flag } from "./re"
import { ro_flag } from "./ro"
import { rs_flag } from "./rs"
import { ru_flag } from "./ru"
import { rw_flag } from "./rw"
import { sa_flag } from "./sa"
import { sb_flag } from "./sb"
import { sc_flag } from "./sc"
import { sd_flag } from "./sd"
import { se_flag } from "./se"
import { sg_flag } from "./sg"
import { sh_ac_flag } from "./sh-ac"
import { sh_hl_flag } from "./sh-hl"
import { sh_ta_flag } from "./sh-ta"
import { sh_flag } from "./sh"
import { si_flag } from "./si"
import { sj_flag } from "./sj"
import { sk_flag } from "./sk"
import { sl_flag } from "./sl"
import { sm_flag } from "./sm"
import { sn_flag } from "./sn"
import { so_flag } from "./so"
import { sr_flag } from "./sr"
import { ss_flag } from "./ss"
import { st_flag } from "./st"
import { sv_flag } from "./sv"
import { sx_flag } from "./sx"
import { sy_flag } from "./sy"
import { sz_flag } from "./sz"
import { tc_flag } from "./tc"
import { td_flag } from "./td"
import { tf_flag } from "./tf"
import { tg_flag } from "./tg"
import { th_flag } from "./th"
import { tj_flag } from "./tj"
import { tk_flag } from "./tk"
import { tl_flag } from "./tl"
import { tm_flag } from "./tm"
import { tn_flag } from "./tn"
import { to_flag } from "./to"
import { tr_flag } from "./tr"
import { tt_flag } from "./tt"
import { tv_flag } from "./tv"
import { tw_flag } from "./tw"
import { tz_flag } from "./tz"
import { ua_flag } from "./ua"
import { ug_flag } from "./ug"
import { um_flag } from "./um"
import { un_flag } from "./un"
import { us_flag } from "./us"
import { uy_flag } from "./uy"
import { uz_flag } from "./uz"
import { va_flag } from "./va"
import { vc_flag } from "./vc"
import { ve_flag } from "./ve"
import { vg_flag } from "./vg"
import { vi_flag } from "./vi"
import { vn_flag } from "./vn"
import { vu_flag } from "./vu"
import { wf_flag } from "./wf"
import { ws_flag } from "./ws"
import { xk_flag } from "./xk"
import { xx_flag } from "./xx"
import { ye_flag } from "./ye"
import { yt_flag } from "./yt"
import { za_flag } from "./za"
import { zm_flag } from "./zm"
import { zw_flag } from "./zw"

export function nameToFlagIcon(iconName: string, format: "1x1" | "4x3" = "1x1", props: IconBaseProps = {}, className: string = ""): IconType | undefined {

    if (!iconName.startsWith("nd-flag-icons/")) {
        return undefined;
    }

    const countryCode = iconName.substring("nd-flag-icons/".length);

    if ("ad" === countryCode) {
        return ad_flag(format, className);
    }
    if ("ae" === countryCode) {
        return ae_flag(format, className);
    }
    if ("af" === countryCode) {
        return af_flag(format, className);
    }
    if ("ag" === countryCode) {
        return ag_flag(format, className);
    }
    if ("ai" === countryCode) {
        return ai_flag(format, className);
    }
    if ("al" === countryCode) {
        return al_flag(format, className);
    }
    if ("am" === countryCode) {
        return am_flag(format, className);
    }
    if ("ao" === countryCode) {
        return ao_flag(format, className);
    }
    if ("aq" === countryCode) {
        return aq_flag(format, className);
    }
    if ("ar" === countryCode) {
        return ar_flag(format, className);
    }
    if ("arab" === countryCode) {
        return arab_flag(format, className);
    }
    if ("as" === countryCode) {
        return as_flag(format, className);
    }
    if ("at" === countryCode) {
        return at_flag(format, className);
    }
    if ("au" === countryCode) {
        return au_flag(format, className);
    }
    if ("aw" === countryCode) {
        return aw_flag(format, className);
    }
    if ("ax" === countryCode) {
        return ax_flag(format, className);
    }
    if ("az" === countryCode) {
        return az_flag(format, className);
    }
    if ("ba" === countryCode) {
        return ba_flag(format, className);
    }
    if ("bb" === countryCode) {
        return bb_flag(format, className);
    }
    if ("bd" === countryCode) {
        return bd_flag(format, className);
    }
    if ("be" === countryCode) {
        return be_flag(format, className);
    }
    if ("bf" === countryCode) {
        return bf_flag(format, className);
    }
    if ("bg" === countryCode) {
        return bg_flag(format, className);
    }
    if ("bh" === countryCode) {
        return bh_flag(format, className);
    }
    if ("bi" === countryCode) {
        return bi_flag(format, className);
    }
    if ("bj" === countryCode) {
        return bj_flag(format, className);
    }
    if ("bl" === countryCode) {
        return bl_flag(format, className);
    }
    if ("bm" === countryCode) {
        return bm_flag(format, className);
    }
    if ("bn" === countryCode) {
        return bn_flag(format, className);
    }
    if ("bo" === countryCode) {
        return bo_flag(format, className);
    }
    if ("bq" === countryCode) {
        return bq_flag(format, className);
    }
    if ("br" === countryCode) {
        return br_flag(format, className);
    }
    if ("bs" === countryCode) {
        return bs_flag(format, className);
    }
    if ("bt" === countryCode) {
        return bt_flag(format, className);
    }
    if ("bv" === countryCode) {
        return bv_flag(format, className);
    }
    if ("bw" === countryCode) {
        return bw_flag(format, className);
    }
    if ("by" === countryCode) {
        return by_flag(format, className);
    }
    if ("bz" === countryCode) {
        return bz_flag(format, className);
    }
    if ("ca" === countryCode) {
        return ca_flag(format, className);
    }
    if ("cc" === countryCode) {
        return cc_flag(format, className);
    }
    if ("cd" === countryCode) {
        return cd_flag(format, className);
    }
    if ("cefta" === countryCode) {
        return cefta_flag(format, className);
    }
    if ("cf" === countryCode) {
        return cf_flag(format, className);
    }
    if ("cg" === countryCode) {
        return cg_flag(format, className);
    }
    if ("ch" === countryCode) {
        return ch_flag(format, className);
    }
    if ("ci" === countryCode) {
        return ci_flag(format, className);
    }
    if ("ck" === countryCode) {
        return ck_flag(format, className);
    }
    if ("cl" === countryCode) {
        return cl_flag(format, className);
    }
    if ("cm" === countryCode) {
        return cm_flag(format, className);
    }
    if ("cn" === countryCode) {
        return cn_flag(format, className);
    }
    if ("co" === countryCode) {
        return co_flag(format, className);
    }
    if ("cp" === countryCode) {
        return cp_flag(format, className);
    }
    if ("cr" === countryCode) {
        return cr_flag(format, className);
    }
    if ("cu" === countryCode) {
        return cu_flag(format, className);
    }
    if ("cv" === countryCode) {
        return cv_flag(format, className);
    }
    if ("cw" === countryCode) {
        return cw_flag(format, className);
    }
    if ("cx" === countryCode) {
        return cx_flag(format, className);
    }
    if ("cy" === countryCode) {
        return cy_flag(format, className);
    }
    if ("cz" === countryCode) {
        return cz_flag(format, className);
    }
    if ("de" === countryCode) {
        return de_flag(format, className);
    }
    if ("dg" === countryCode) {
        return dg_flag(format, className);
    }
    if ("dj" === countryCode) {
        return dj_flag(format, className);
    }
    if ("dk" === countryCode) {
        return dk_flag(format, className);
    }
    if ("dm" === countryCode) {
        return dm_flag(format, className);
    }
    if ("do" === countryCode) {
        return do_flag(format, className);
    }
    if ("dz" === countryCode) {
        return dz_flag(format, className);
    }
    if ("eac" === countryCode) {
        return eac_flag(format, className);
    }
    if ("ec" === countryCode) {
        return ec_flag(format, className);
    }
    if ("ee" === countryCode) {
        return ee_flag(format, className);
    }
    if ("eg" === countryCode) {
        return eg_flag(format, className);
    }
    if ("eh" === countryCode) {
        return eh_flag(format, className);
    }
    if ("er" === countryCode) {
        return er_flag(format, className);
    }
    if ("es-ct" === countryCode) {
        return es_ct_flag(format, className);
    }
    if ("es-ga" === countryCode) {
        return es_ga_flag(format, className);
    }
    if ("es-pv" === countryCode) {
        return es_pv_flag(format, className);
    }
    if ("es" === countryCode) {
        return es_flag(format, className);
    }
    if ("et" === countryCode) {
        return et_flag(format, className);
    }
    if ("eu" === countryCode) {
        return eu_flag(format, className);
    }
    if ("fi" === countryCode) {
        return fi_flag(format, className);
    }
    if ("fj" === countryCode) {
        return fj_flag(format, className);
    }
    if ("fk" === countryCode) {
        return fk_flag(format, className);
    }
    if ("fm" === countryCode) {
        return fm_flag(format, className);
    }
    if ("fo" === countryCode) {
        return fo_flag(format, className);
    }
    if ("fr" === countryCode) {
        return fr_flag(format, className);
    }
    if ("ga" === countryCode) {
        return ga_flag(format, className);
    }
    if ("gb-eng" === countryCode) {
        return gb_eng_flag(format, className);
    }
    if ("gb-nir" === countryCode) {
        return gb_nir_flag(format, className);
    }
    if ("gb-sct" === countryCode) {
        return gb_sct_flag(format, className);
    }
    if ("gb-wls" === countryCode) {
        return gb_wls_flag(format, className);
    }
    if ("gb" === countryCode) {
        return gb_flag(format, className);
    }
    if ("gd" === countryCode) {
        return gd_flag(format, className);
    }
    if ("ge" === countryCode) {
        return ge_flag(format, className);
    }
    if ("gf" === countryCode) {
        return gf_flag(format, className);
    }
    if ("gg" === countryCode) {
        return gg_flag(format, className);
    }
    if ("gh" === countryCode) {
        return gh_flag(format, className);
    }
    if ("gi" === countryCode) {
        return gi_flag(format, className);
    }
    if ("gl" === countryCode) {
        return gl_flag(format, className);
    }
    if ("gm" === countryCode) {
        return gm_flag(format, className);
    }
    if ("gn" === countryCode) {
        return gn_flag(format, className);
    }
    if ("gp" === countryCode) {
        return gp_flag(format, className);
    }
    if ("gq" === countryCode) {
        return gq_flag(format, className);
    }
    if ("gr" === countryCode) {
        return gr_flag(format, className);
    }
    if ("gs" === countryCode) {
        return gs_flag(format, className);
    }
    if ("gt" === countryCode) {
        return gt_flag(format, className);
    }
    if ("gu" === countryCode) {
        return gu_flag(format, className);
    }
    if ("gw" === countryCode) {
        return gw_flag(format, className);
    }
    if ("gy" === countryCode) {
        return gy_flag(format, className);
    }
    if ("hk" === countryCode) {
        return hk_flag(format, className);
    }
    if ("hm" === countryCode) {
        return hm_flag(format, className);
    }
    if ("hn" === countryCode) {
        return hn_flag(format, className);
    }
    if ("hr" === countryCode) {
        return hr_flag(format, className);
    }
    if ("ht" === countryCode) {
        return ht_flag(format, className);
    }
    if ("hu" === countryCode) {
        return hu_flag(format, className);
    }
    if ("ic" === countryCode) {
        return ic_flag(format, className);
    }
    if ("id" === countryCode) {
        return id_flag(format, className);
    }
    if ("ie" === countryCode) {
        return ie_flag(format, className);
    }
    if ("il" === countryCode) {
        return il_flag(format, className);
    }
    if ("im" === countryCode) {
        return im_flag(format, className);
    }
    if ("in" === countryCode) {
        return in_flag(format, className);
    }
    if ("io" === countryCode) {
        return io_flag(format, className);
    }
    if ("iq" === countryCode) {
        return iq_flag(format, className);
    }
    if ("ir" === countryCode) {
        return ir_flag(format, className);
    }
    if ("is" === countryCode) {
        return is_flag(format, className);
    }
    if ("it" === countryCode) {
        return it_flag(format, className);
    }
    if ("je" === countryCode) {
        return je_flag(format, className);
    }
    if ("jm" === countryCode) {
        return jm_flag(format, className);
    }
    if ("jo" === countryCode) {
        return jo_flag(format, className);
    }
    if ("jp" === countryCode) {
        return jp_flag(format, className);
    }
    if ("ke" === countryCode) {
        return ke_flag(format, className);
    }
    if ("kg" === countryCode) {
        return kg_flag(format, className);
    }
    if ("kh" === countryCode) {
        return kh_flag(format, className);
    }
    if ("ki" === countryCode) {
        return ki_flag(format, className);
    }
    if ("km" === countryCode) {
        return km_flag(format, className);
    }
    if ("kn" === countryCode) {
        return kn_flag(format, className);
    }
    if ("kp" === countryCode) {
        return kp_flag(format, className);
    }
    if ("kr" === countryCode) {
        return kr_flag(format, className);
    }
    if ("kw" === countryCode) {
        return kw_flag(format, className);
    }
    if ("ky" === countryCode) {
        return ky_flag(format, className);
    }
    if ("kz" === countryCode) {
        return kz_flag(format, className);
    }
    if ("la" === countryCode) {
        return la_flag(format, className);
    }
    if ("lb" === countryCode) {
        return lb_flag(format, className);
    }
    if ("lc" === countryCode) {
        return lc_flag(format, className);
    }
    if ("li" === countryCode) {
        return li_flag(format, className);
    }
    if ("lk" === countryCode) {
        return lk_flag(format, className);
    }
    if ("lr" === countryCode) {
        return lr_flag(format, className);
    }
    if ("ls" === countryCode) {
        return ls_flag(format, className);
    }
    if ("lt" === countryCode) {
        return lt_flag(format, className);
    }
    if ("lu" === countryCode) {
        return lu_flag(format, className);
    }
    if ("lv" === countryCode) {
        return lv_flag(format, className);
    }
    if ("ly" === countryCode) {
        return ly_flag(format, className);
    }
    if ("ma" === countryCode) {
        return ma_flag(format, className);
    }
    if ("mc" === countryCode) {
        return mc_flag(format, className);
    }
    if ("md" === countryCode) {
        return md_flag(format, className);
    }
    if ("me" === countryCode) {
        return me_flag(format, className);
    }
    if ("mf" === countryCode) {
        return mf_flag(format, className);
    }
    if ("mg" === countryCode) {
        return mg_flag(format, className);
    }
    if ("mh" === countryCode) {
        return mh_flag(format, className);
    }
    if ("mk" === countryCode) {
        return mk_flag(format, className);
    }
    if ("ml" === countryCode) {
        return ml_flag(format, className);
    }
    if ("mm" === countryCode) {
        return mm_flag(format, className);
    }
    if ("mn" === countryCode) {
        return mn_flag(format, className);
    }
    if ("mo" === countryCode) {
        return mo_flag(format, className);
    }
    if ("mp" === countryCode) {
        return mp_flag(format, className);
    }
    if ("mq" === countryCode) {
        return mq_flag(format, className);
    }
    if ("mr" === countryCode) {
        return mr_flag(format, className);
    }
    if ("ms" === countryCode) {
        return ms_flag(format, className);
    }
    if ("mt" === countryCode) {
        return mt_flag(format, className);
    }
    if ("mu" === countryCode) {
        return mu_flag(format, className);
    }
    if ("mv" === countryCode) {
        return mv_flag(format, className);
    }
    if ("mw" === countryCode) {
        return mw_flag(format, className);
    }
    if ("mx" === countryCode) {
        return mx_flag(format, className);
    }
    if ("my" === countryCode) {
        return my_flag(format, className);
    }
    if ("mz" === countryCode) {
        return mz_flag(format, className);
    }
    if ("na" === countryCode) {
        return na_flag(format, className);
    }
    if ("nc" === countryCode) {
        return nc_flag(format, className);
    }
    if ("ne" === countryCode) {
        return ne_flag(format, className);
    }
    if ("nf" === countryCode) {
        return nf_flag(format, className);
    }
    if ("ng" === countryCode) {
        return ng_flag(format, className);
    }
    if ("ni" === countryCode) {
        return ni_flag(format, className);
    }
    if ("nl" === countryCode) {
        return nl_flag(format, className);
    }
    if ("no" === countryCode) {
        return no_flag(format, className);
    }
    if ("np" === countryCode) {
        return np_flag(format, className);
    }
    if ("nr" === countryCode) {
        return nr_flag(format, className);
    }
    if ("nu" === countryCode) {
        return nu_flag(format, className);
    }
    if ("nz" === countryCode) {
        return nz_flag(format, className);
    }
    if ("om" === countryCode) {
        return om_flag(format, className);
    }
    if ("pa" === countryCode) {
        return pa_flag(format, className);
    }
    if ("pc" === countryCode) {
        return pc_flag(format, className);
    }
    if ("pe" === countryCode) {
        return pe_flag(format, className);
    }
    if ("pf" === countryCode) {
        return pf_flag(format, className);
    }
    if ("pg" === countryCode) {
        return pg_flag(format, className);
    }
    if ("ph" === countryCode) {
        return ph_flag(format, className);
    }
    if ("pk" === countryCode) {
        return pk_flag(format, className);
    }
    if ("pl" === countryCode) {
        return pl_flag(format, className);
    }
    if ("pm" === countryCode) {
        return pm_flag(format, className);
    }
    if ("pn" === countryCode) {
        return pn_flag(format, className);
    }
    if ("pr" === countryCode) {
        return pr_flag(format, className);
    }
    if ("ps" === countryCode) {
        return ps_flag(format, className);
    }
    if ("pt" === countryCode) {
        return pt_flag(format, className);
    }
    if ("pw" === countryCode) {
        return pw_flag(format, className);
    }
    if ("py" === countryCode) {
        return py_flag(format, className);
    }
    if ("qa" === countryCode) {
        return qa_flag(format, className);
    }
    if ("re" === countryCode) {
        return re_flag(format, className);
    }
    if ("ro" === countryCode) {
        return ro_flag(format, className);
    }
    if ("rs" === countryCode) {
        return rs_flag(format, className);
    }
    if ("ru" === countryCode) {
        return ru_flag(format, className);
    }
    if ("rw" === countryCode) {
        return rw_flag(format, className);
    }
    if ("sa" === countryCode) {
        return sa_flag(format, className);
    }
    if ("sb" === countryCode) {
        return sb_flag(format, className);
    }
    if ("sc" === countryCode) {
        return sc_flag(format, className);
    }
    if ("sd" === countryCode) {
        return sd_flag(format, className);
    }
    if ("se" === countryCode) {
        return se_flag(format, className);
    }
    if ("sg" === countryCode) {
        return sg_flag(format, className);
    }
    if ("sh-ac" === countryCode) {
        return sh_ac_flag(format, className);
    }
    if ("sh-hl" === countryCode) {
        return sh_hl_flag(format, className);
    }
    if ("sh-ta" === countryCode) {
        return sh_ta_flag(format, className);
    }
    if ("sh" === countryCode) {
        return sh_flag(format, className);
    }
    if ("si" === countryCode) {
        return si_flag(format, className);
    }
    if ("sj" === countryCode) {
        return sj_flag(format, className);
    }
    if ("sk" === countryCode) {
        return sk_flag(format, className);
    }
    if ("sl" === countryCode) {
        return sl_flag(format, className);
    }
    if ("sm" === countryCode) {
        return sm_flag(format, className);
    }
    if ("sn" === countryCode) {
        return sn_flag(format, className);
    }
    if ("so" === countryCode) {
        return so_flag(format, className);
    }
    if ("sr" === countryCode) {
        return sr_flag(format, className);
    }
    if ("ss" === countryCode) {
        return ss_flag(format, className);
    }
    if ("st" === countryCode) {
        return st_flag(format, className);
    }
    if ("sv" === countryCode) {
        return sv_flag(format, className);
    }
    if ("sx" === countryCode) {
        return sx_flag(format, className);
    }
    if ("sy" === countryCode) {
        return sy_flag(format, className);
    }
    if ("sz" === countryCode) {
        return sz_flag(format, className);
    }
    if ("tc" === countryCode) {
        return tc_flag(format, className);
    }
    if ("td" === countryCode) {
        return td_flag(format, className);
    }
    if ("tf" === countryCode) {
        return tf_flag(format, className);
    }
    if ("tg" === countryCode) {
        return tg_flag(format, className);
    }
    if ("th" === countryCode) {
        return th_flag(format, className);
    }
    if ("tj" === countryCode) {
        return tj_flag(format, className);
    }
    if ("tk" === countryCode) {
        return tk_flag(format, className);
    }
    if ("tl" === countryCode) {
        return tl_flag(format, className);
    }
    if ("tm" === countryCode) {
        return tm_flag(format, className);
    }
    if ("tn" === countryCode) {
        return tn_flag(format, className);
    }
    if ("to" === countryCode) {
        return to_flag(format, className);
    }
    if ("tr" === countryCode) {
        return tr_flag(format, className);
    }
    if ("tt" === countryCode) {
        return tt_flag(format, className);
    }
    if ("tv" === countryCode) {
        return tv_flag(format, className);
    }
    if ("tw" === countryCode) {
        return tw_flag(format, className);
    }
    if ("tz" === countryCode) {
        return tz_flag(format, className);
    }
    if ("ua" === countryCode) {
        return ua_flag(format, className);
    }
    if ("ug" === countryCode) {
        return ug_flag(format, className);
    }
    if ("um" === countryCode) {
        return um_flag(format, className);
    }
    if ("un" === countryCode) {
        return un_flag(format, className);
    }
    if ("us" === countryCode) {
        return us_flag(format, className);
    }
    if ("uy" === countryCode) {
        return uy_flag(format, className);
    }
    if ("uz" === countryCode) {
        return uz_flag(format, className);
    }
    if ("va" === countryCode) {
        return va_flag(format, className);
    }
    if ("vc" === countryCode) {
        return vc_flag(format, className);
    }
    if ("ve" === countryCode) {
        return ve_flag(format, className);
    }
    if ("vg" === countryCode) {
        return vg_flag(format, className);
    }
    if ("vi" === countryCode) {
        return vi_flag(format, className);
    }
    if ("vn" === countryCode) {
        return vn_flag(format, className);
    }
    if ("vu" === countryCode) {
        return vu_flag(format, className);
    }
    if ("wf" === countryCode) {
        return wf_flag(format, className);
    }
    if ("ws" === countryCode) {
        return ws_flag(format, className);
    }
    if ("xk" === countryCode) {
        return xk_flag(format, className);
    }
    if ("xx" === countryCode) {
        return xx_flag(format, className);
    }
    if ("ye" === countryCode) {
        return ye_flag(format, className);
    }
    if ("yt" === countryCode) {
        return yt_flag(format, className);
    }
    if ("za" === countryCode) {
        return za_flag(format, className);
    }
    if ("zm" === countryCode) {
        return zm_flag(format, className);
    }
    if ("zw" === countryCode) {
        return zw_flag(format, className);
    }

    return undefined;
}
