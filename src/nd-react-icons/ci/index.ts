import type {IconType} from "react-icons";
import { nameToReactIcon_ci_0 } from "./name-to-react-icon-ci-0";

export function nameToReactIcon_ci(iconName: string): IconType | undefined {

    return (
        nameToReactIcon_ci_0(iconName) ||
        undefined
    );

}