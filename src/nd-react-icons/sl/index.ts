import type {IconType} from "react-icons";
import { nameToReactIcon_sl_0 } from "./name-to-react-icon-sl-0";

export function nameToReactIcon_sl(iconName: string): IconType | undefined {

    return (
        nameToReactIcon_sl_0(iconName) ||
        undefined
    );

}