import type {IconType} from "react-icons";
import { nameToReactIcon_ai_0 } from "./name-to-react-icon-ai-0";

export function nameToReactIcon_ai(iconName: string): IconType | undefined {

    return (
        nameToReactIcon_ai_0(iconName) ||
        undefined
    );

}