import type {IconType} from "react-icons";
import { nameToReactIcon_im_0 } from "./name-to-react-icon-im-0";

export function nameToReactIcon_im(iconName: string): IconType | undefined {

    return (
        nameToReactIcon_im_0(iconName) ||
        undefined
    );

}