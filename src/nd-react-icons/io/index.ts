import type {IconType} from "react-icons";
import { nameToReactIcon_io_0 } from "./name-to-react-icon-io-0";

export function nameToReactIcon_io(iconName: string): IconType | undefined {

    return (
        nameToReactIcon_io_0(iconName) ||
        undefined
    );

}