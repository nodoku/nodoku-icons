import type {IconType} from "react-icons";
import { nameToReactIcon_go_0 } from "./name-to-react-icon-go-0";

export function nameToReactIcon_go(iconName: string): IconType | undefined {

    return (
        nameToReactIcon_go_0(iconName) ||
        undefined
    );

}