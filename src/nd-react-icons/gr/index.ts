import type {IconType} from "react-icons";
import { nameToReactIcon_gr_0 } from "./name-to-react-icon-gr-0";

export function nameToReactIcon_gr(iconName: string): IconType | undefined {

    return (
        nameToReactIcon_gr_0(iconName) ||
        undefined
    );

}