import type {IconType} from "react-icons";
import { nameToReactIcon_fi_0 } from "./name-to-react-icon-fi-0";

export function nameToReactIcon_fi(iconName: string): IconType | undefined {

    return (
        nameToReactIcon_fi_0(iconName) ||
        undefined
    );

}