import type {IconType} from "react-icons";
import { nameToReactIcon_di_0 } from "./name-to-react-icon-di-0";

export function nameToReactIcon_di(iconName: string): IconType | undefined {

    return (
        nameToReactIcon_di_0(iconName) ||
        undefined
    );

}