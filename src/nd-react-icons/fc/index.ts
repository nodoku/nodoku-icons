import type {IconType} from "react-icons";
import { nameToReactIcon_fc_0 } from "./name-to-react-icon-fc-0";

export function nameToReactIcon_fc(iconName: string): IconType | undefined {

    return (
        nameToReactIcon_fc_0(iconName) ||
        undefined
    );

}